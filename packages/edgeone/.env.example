# 必需：你的 Webflow 项目地址（例如 project-name.webflow.io）
WEBFLOW_HOST=

# 可选：禁止搜索引擎收录的公开域名，多个域名用英文逗号分隔
# 测试期间可同时加入预览域名和待发布正式域名；上线时移除正式域名即可
# NOINDEX_HOSTS=preview.example.com,www.example.com

# 可选：当前 Makers 线路自身的公开域名（不含路径）
# PUBLIC_HOST=tectura-v26-staging.webflowcn.com

# 可选：同一 Makers 项目作为 Site Acceleration 回源时的外部域名
# 只有请求头密钥与 SITE_ACCELERATION_SECRET 完全一致时才生效
# SITE_ACCELERATION_PUBLIC_HOST=tectura-v26-eo.webflowcn.com

# 可选：由 EdgeOne 回源规则注入的随机密钥，只能在控制台设置，禁止提交到 Git
# SITE_ACCELERATION_SECRET=

# 可选：资源代理路径前缀（默认 /__eo_asset_v3__）
# ASSET_PROXY_PREFIX=/__eo_asset_v3__

# 可选：持久 HTML 快照保持新鲜的时间（秒，默认 900）
# SNAPSHOT_TTL=900

# 可选：KV 未绑定时启用 Blob 备用快照；不填则关闭
# SNAPSHOT_BLOB_STORE=edgeflow-snapshots # HTML 备用快照 + 静态资源缓存回退

# 可选：Sitemap 不可用时，主动刷新端点使用的备用页面列表（最多 20 个）
# SNAPSHOT_PATHS=/,/about,/contact

# 可选：保护 POST /__proxy/refresh 的随机密钥（必须通过控制台环境变量设置）
# SNAPSHOT_REFRESH_SECRET=

# KV 绑定不写在本文件中：在 Makers 控制台将命名空间绑定为 EDGEFLOW_SNAPSHOT
# KV 与 Blob 同时可用时，运行时只使用 KV

# 可选：国内加速 CDN 镜像（不填使用默认值）
# MIRROR_JQUERY=https://lib.baomitu.com/jquery/3.5.1/jquery.min.js
# MIRROR_WEBFONT=https://cdn.jsdelivr.net/npm/webfontloader@1.6.26/webfontloader.js
# MIRROR_JSD_MIRROR=https://cdn.jsdmirror.com
